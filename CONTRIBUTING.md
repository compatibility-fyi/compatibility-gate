# Contributing

Contributions are welcome. Keep changes focused, add tests for behavior changes, and use
conventional commits.

Before submitting a pull request, run:

```sh
npm ci
npm run validate
git diff --exit-code -- dist
```

`dist/` is generated by `npm run build` and must be committed so GitHub can execute the action
without installing dependencies. Do not edit bundled files manually.
